---
trigger: always_on
---

# Antigravity Context

**IMPORTANT**: Read this file at the start of every task to preserve tokens instead of re-analyzing the full codebase.

## Sample Data Layout
The target CSV log (`samples\colesdata.csv`) format:
- **Row 1**: Metadata (e.g., `Report Generated: 29/05/2026      06:06:22 PM (AEST),,,,,,,`) - **Must be skipped.**
- **Row 2**: Headers - **Must be skipped or used as keys.**
- **Row 3+**: Data rows

### Explicit Column Names to Parse
- `Event`
- `To`
- `Date` (Format: MM/DD/YYYY)
- `Time`
- `Duration (Sec)`
- `Data Up (KB)`
- `Data Down (KB)`
- `Price`

## Active State Framework
The application has three primary View Modes:
1. **Raw**: Full unaggregated list of all log entries.
2. **Daily**: Grouped and sorted strictly by day. Aggregates data usage, duration, etc. 
   - **Daily -> Event Drill-down Toggle**: Clicking any Day row expands a nested table showing all individual events for that day.
3. **Monthly**: Grouped by Month/Year (e.g., May 2026). 
   - **Monthly -> Daily Drill-down Toggle**: Clicking any Month row expands a nested table showing the Daily Breakdown.
   - **Daily -> Event Drill-down Toggle**: Inside the Daily Breakdown, clicking any Day row further expands to show all individual events for that day.

## Design System & Theme
- **SaaS Dark Mode**: The UI should utilize a gorgeous dark theme specifically using Tailwind classes: `bg-zinc-950`, `text-zinc-50`, `border-zinc-800`.
- **Table Capabilities**: Clickable header sorting (handling string vs mathematical numeric sorting automatically), sticky table headers, and a global search/filter bar for all views.

## Agent Constraints & Preferences
- **Styling**: Always write Tailwind utility classes inline; do not use external CSS files.
- **Data Parsing**: Use PapaParse's streaming option (`step` function) to ensure Objective 1 (Large CSV efficiency) is met.
- **State Management**: Prefer React's native `useState` and `useContext` before pulling in external state libraries.
